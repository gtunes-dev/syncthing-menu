### Added
- Activity rows show a file, folder, or link icon beside the name.

### Changed
- Every file change is listed individually in the Activity log, as it happens; large batches are no longer summarized as "N changes" rows, so any file can be found by search while it is still in the log.
- The Activity log keeps the most recent 5,000 rows (previously 500).

