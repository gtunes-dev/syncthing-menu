<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundleDisplayName</key>
	<string>Syncthing Menu</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>LSUIElement</key>
	<true/>
	<key>NSHumanReadableCopyright</key>
	<string>Copyright © 2026 Greg Friedman. All rights reserved.</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- Sparkle (app self-update). The feed lives on GitHub Pages in production;
	     SparkleUpdateSource overrides it with a local appcast for testing. -->
	<key>SUFeedURL</key>
	<string>https://gtunes-dev.github.io/syncthing-menu/appcast.xml</string>
	<key>SUPublicEDKey</key>
	<string>Z03dL2Vf4gVMfWlAifARn9KxUrNLP7gMQW4RTggB9+A=</string>
	<!-- Sensible defaults; the user toggles these through our Settings UI. Setting
	     SUEnableAutomaticChecks here suppresses Sparkle's own first-run prompt. -->
	<key>SUEnableAutomaticChecks</key>
	<true/>
	<!-- Required for the silent install path. We turn automaticallyChecksForUpdates
	     off at runtime (we drive check cadence ourselves); without this key Sparkle
	     derives its allows-automatic-updates gate FROM that setting and silently
	     ignores automaticallyDownloadsUpdates = true, routing every install through
	     its interactive update alert instead of background download + silent install. -->
	<key>SUAllowsAutomaticUpdates</key>
	<true/>
	<key>SUScheduledCheckInterval</key>
	<integer>86400</integer>
</dict>
</plist>
